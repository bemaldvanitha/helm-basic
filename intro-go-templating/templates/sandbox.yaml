# I am yaml comment
{{/* i am a go comment */}}

test: {{ .Values.test }}
labels:
  {{/* app label from release */}}
  app: {{ .Release.Name }}
  {{/* app label from chart */}}
  chart: {{ .Chart.Name }}